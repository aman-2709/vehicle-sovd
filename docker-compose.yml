---
# Docker Compose configuration for SOVD Command WebApp
# This file will be populated in task I1.T5
# Placeholder for local development environment orchestration

version: '3.8'

services:
  placeholder:
    image: hello-world
    # This is a placeholder service. Actual services will be defined in I1.T5
    # Services to be added:
    # - frontend (React app)
    # - backend (FastAPI app)
    # - db (PostgreSQL)
    # - redis (Redis cache/pub-sub)

networks:
  sovd-network:
    driver: bridge

volumes:
  db_data:
