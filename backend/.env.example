# Database Configuration
# Format: postgresql+asyncpg://user:password@host:port/database
# For Docker: use service name (db) as host
# For local development: use localhost
DATABASE_URL=postgresql+asyncpg://sovd_user:sovd_pass@localhost:5432/sovd

# Redis Configuration
# Format: redis://host:port/db_number
# For Docker: use service name (redis) as host
# For local development: use localhost
REDIS_URL=redis://localhost:6379/0

# JWT Authentication Configuration
# IMPORTANT: Generate a secure random string for production
# Example: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# JWT Algorithm (default: HS256)
# Common options: HS256, HS384, HS512, RS256
JWT_ALGORITHM=HS256

# JWT Token Expiration (in minutes, default: 15)
JWT_EXPIRATION_MINUTES=15
