# SOVD Command WebApp - Environment Configuration Template
# Copy this file to .env and replace placeholder values with actual credentials

# Database Configuration
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://sovd_user:change_me_password@localhost:5432/sovd_db

# Redis Configuration
# Redis connection string for session storage and caching
# Format: redis://host:port/db_number
REDIS_URL=redis://localhost:6379/0

# JWT Authentication Configuration
# CRITICAL: Change JWT_SECRET in production to a cryptographically secure random string
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET=your-secret-key-CHANGE-IN-PRODUCTION-use-long-random-string
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=15

# Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Production: Use specific domains (e.g., https://sovd.example.com)
# Development: http://localhost:3000
# NEVER use wildcard (*) in production
CORS_ORIGINS=http://localhost:3000

# Security Notes:
# - Never commit .env file to version control (it's in .gitignore)
# - Use environment-specific values (dev, staging, prod)
# - Rotate JWT_SECRET regularly in production
# - Use AWS Secrets Manager or similar for production secrets
# - Ensure DATABASE_URL uses strong passwords (16+ chars, mixed case, symbols)
